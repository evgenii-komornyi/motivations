import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    lottie: {
        width: 200,
        height: 200,
    },
    button: {
        width: 40,
        height: 40,
        padding: 8,
        justifyContent: 'center',
        alignItems: 'center',
        borderWidth: 1,
        borderColor: 'white',
        borderRadius: 35,
    },
    buttonText: {
        paddingLeft: 10,
    },
});
